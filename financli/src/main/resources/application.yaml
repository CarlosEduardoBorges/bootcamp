spring:
  application:
    name: financli
  datasource:
    url: jdbc:h2:file:./data/financli
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  main:
    banner-mode: off
logging:
  level:
    root: WARN
    org.springframework: WARN